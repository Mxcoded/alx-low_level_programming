#include <stdio.h>
/**
*main-This program print alphabets
*Return: always 0 i.e main function run successfully
*/
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	return (0);
}
